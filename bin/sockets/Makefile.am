# Rules for the test code (use `make check` to execute)
TESTS = acamServerTest
bin_PROGRAMS = server client
#check_PROGRAMS = $(TESTS)
server_SOURCES = winlinuxsockserver.c
client_SOURCES = winlinuxsockclient.c
#winlinuxsock_CXXFLAGS = $(CPPUNIT_CFLAGS)
#winlinuxsock_LDFLAGS = -lsocket -lnsl

